services:
  bot:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: controlador-gastos
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
